perm free

var[str] my_var = "testing this variable";
var[str] command = get_args()[0];

hide_cursor();
clear();
change_term_title("Skrillec NET");
set_term_size(30,87);
output_wrfx("wrfx/ui.wrfx");
sleep(500,millisecond);
slow_place_text(15,6,250,millisecond,"test....");
print_text("{my_var}");
//include_whfx("assets/wockyfx/whfx/clear_box.whfx").clear();
place_text(20,6,"testing back to wfx file data again {command}");
sleep(500,millisecond);
show_cursor();
sleep(10,second);