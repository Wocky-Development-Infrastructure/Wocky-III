perm premium

set_max_arg(5);
set_arg_err_msg(fn() => {
    perm free
    clear();
    output_wrfx("ui");
    list_text(13,9,"[x] Error....Invalid argument{NEWLINE}Usage: attack <ip> <port> <time> <method>");
});

clear();
output_wrfx("ui");
place_text(13,9,"Attack Sent");